@model FitGym.Web.ViewModels.Forum.IndexViewModel
@{
    this.ViewData["Title"] = "Categories";
}
<h1>@this.ViewData["Title"]</h1>

<p>This is the gyms forum page.</p>
@foreach (var category in Model.Categories)
{
    <p>@category.Name</p>
}