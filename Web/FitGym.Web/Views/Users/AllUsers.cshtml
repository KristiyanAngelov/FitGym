@model AllUsersViewModel

@using FitGym.Data.Models
@using Microsoft.AspNetCore.Identity

<h1>All Users</h1>

@*@foreach (var user in Model)
    {
        <div class="card mb-3">
            <div class="card-header">
                User : @user.FirstName @user.LastName
            </div>
            <div class="card-body">
                <h5 class="card-title"><img src="@user.ProfilePictureUrl" width="200" class="mr-3" alt="@user.UserName">@user.UserName</h5>
            </div>
            <div class="card-footer">
                <a asp-controller="Users" asp-action="GetUserRoles" asp-route-id="@user.Id" class="btn btn-primary">Edit Roles</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>
        </div>
    }*@

<table class="table table-dark">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Id</th>
            <th scope="col">First Name</th>
            <th scope="col">Last Name</th>
            <th scope="col">Email</th>
            <th scope="col">Description</th>
            <th scope="col">#</th>
        </tr>
    </thead>
    @foreach (var user in Model.Users)
    {
        <tbody>
            <tr>
                <td> <img src="@user.ProfilePictureUrl" width="200" class="mr-3" alt="@user.UserName"></td>
                <td>@user.Id</td>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td>@user.Email</td>
                <td>@user.Description</td>
                <td>
                <a href="#" class="btn btn-danger">Edit</a> <br />
                <br /><a class="btn btn-danger" asp-controller="Users" asp-action="DeleteUser" asp-route-id="@user.Id">Delete</a></td>
            </tr>
        </tbody>
    }
</table>
